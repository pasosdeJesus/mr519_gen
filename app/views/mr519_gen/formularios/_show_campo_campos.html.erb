<%=  
  if @registro.campos
    r = ""
    num = 1
    @registro.campos.each do |x|
      r += "(#{num}) #{x.nombre} : 
      #{Sip::ModeloHelper.etiqueta_coleccion(
      Mr519Gen::ApplicationHelper::TIPOS_CAMPO, x.tipo)}
      #{x.obligatorio ? '*' : ''} "
      num += 1
    end
    r
  end
%>
